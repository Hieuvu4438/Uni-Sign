# Copy outside Git as .env and set an unguessable value. Do not commit it.
SERVICE_API_KEY=replace-with-a-long-random-service-secret

# Parent directory containing immutable model-release folders.
MODEL_RELEASES_DIR=/srv/unisign/models
